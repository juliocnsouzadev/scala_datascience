package week4.primitives

abstract class Boolean {
  
  def ifThenElse[T](t: => T, f: => T) : T
  
  def && (x: => Boolean): Boolean =
     ifThenElse(x, False)
  
     def || (x: => Boolean): Boolean =
       ifThenElse(False, x)
}