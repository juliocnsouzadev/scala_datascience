 package week3

object insets {
  println("Welcome to the Scala worksheet")
}

abstract class IntSet {
	def add(x: Int): IntSet
	def contains(x: Int): Boolean
}

class Empty extends IntSet {
	def add(x: Int): IntSet
	def contains(x: Int): Boolean = false
}

class NomEmpty extends IntSet {
	def add(x: Int): IntSet
	def contains(x: Int): Boolean = false
}